name = "aerial-viewer"
main = "src/worker.js"
compatibility_date = "2024-11-01"

# Optional: enable Node-like globals if your script uses them
compatibility_flags = ["nodejs_compat"]

# You can deploy to a specific route if you have a domain set up,
# or leave these commented out for development/preview mode.
# routes = [
#   { pattern = "aerial.example.com/*", zone_name = "example.com" }
# ]

[vars]
API_BASE = "https://services.thelist.tas.gov.au/arcgis/rest/services"
MAPSERVER_PATH = "/Public/AerialPhoto_TimeV2/MapServer/0/query"

[dev]
ip = "0.0.0.0"
port = 8787
local_protocol = "http"
